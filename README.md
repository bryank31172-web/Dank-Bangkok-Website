# D.T.S Industry — Website
Static single-page site + Vercel serverless chatbot proxy.

Local preview: open `index.html` in a browser (chatbot needs deploy).
Deploy: push to GitHub → import in Vercel → add env var `ANTHROPIC_API_KEY`.
See `CLAUDE.md` for full project context.
