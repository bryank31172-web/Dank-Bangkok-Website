# Beam Checkout — วิธีเปิดใช้ · setup

รับเงินผ่าน **Beam** ([beamcheckout.com](https://beamcheckout.com)) แทน Omise
Bryan เลือกอันนี้เมื่อ 25 ส.ค. 2026 เพราะ Omise สมัครมาเป็นเดือนแล้วยังไม่ผ่าน
และ **PromptPay ผ่าน Beam ไม่เสียค่าธรรมเนียม (0%)** ซึ่งเป็นวิธีที่ลูกค้าร้านนี้
จ่ายกันเกือบทั้งหมด · บัตรเครดิตเริ่มที่ประมาณ 1.8%

โค้ดพร้อมแล้ว เหลือแค่ใส่กุญแจ 3 ค่าใน Vercel แล้ว Redeploy

---

## 1. เอากุญแจจาก Beam

Beam dashboard → **Developers** → **API keys** จะได้ 2 ค่า:

| ใส่ใน Vercel ชื่อนี้ | เอามาจาก |
|---|---|
| `BEAM_MERCHANT_ID` | Merchant ID |
| `BEAM_API_KEY` | Secret API key |

⚠️ `BEAM_API_KEY` เป็นความลับ **ห้ามเอาไปแปะที่ไหนที่คนอื่นเห็น** — ใส่ใน Vercel
เท่านั้น ไม่ต้องส่งมาให้ผมในแชท ผมไม่ต้องเห็นมันก็ทำงานได้

## 2. ตั้ง `WEBHOOK_SECRET` (ข้อนี้ห้ามข้าม)

ถ้าไม่ตั้ง **ลูกค้าจ่ายเงินแล้วเว็บจะไม่รู้** เงินเข้าบัญชีจริง แต่ออเดอร์ยังขึ้นว่า
ยังไม่จ่าย — ซึ่งแย่กว่าจ่ายไม่ได้ เพราะดูเหมือนลูกค้าโกหก

ตั้งเป็นตัวเลข/ตัวอักษรมั่วๆ ยาวๆ อะไรก็ได้ที่เดาไม่ได้ แล้วเอาไปต่อท้าย URL ใน
ข้อถัดไป (ถ้าตั้งไว้แล้วสำหรับ GBPrimePay ใช้ตัวเดิมได้เลย)

## 3. บอก Beam ว่าให้แจ้งเว็บที่ไหน

Beam dashboard → **Developers** → **Webhooks** → ใส่:

```
https://www.dankbangkok.com/api/beam-webhook?secret=<ค่า WEBHOOK_SECRET ของคุณ>
```

## 4. Redeploy

Vercel → Deployments → อันบนสุด → `···` → **Redeploy**
(ตัวแปรใหม่ไม่มีผลจนกว่าจะ redeploy)

เช็คว่าติดแล้ว: เปิด `dankbangkok.com/api/health` มองหา `"beam": true`
ถ้ามีอะไรตั้งไม่ครบ หน้านั้นจะบอกเองใน `warnings`

---

## 5. ⚠️ ทดสอบด้วยเงินจริงจำนวนน้อย ก่อนเปิดให้ลูกค้า

**ข้อนี้สำคัญที่สุด และห้ามข้าม**

ผมเขียนโค้ดโดยอ่านเอกสารของ Beam จากผลการค้นหา เพราะ sandbox ที่ผมทำงานอยู่
เปิด `docs.beamcheckout.com` ตรงๆ ไม่ได้ (network policy บล็อก) สิ่งที่ผม
**ยืนยันจากเอกสารของ Beam เองไม่ได้** คือ ช่อง `amount` ที่ส่งไปให้ Beam นับเป็น
**สตางค์** หรือ **บาท**

ผมเขียนเป็น**สตางค์** เพราะเป็นมาตรฐานของ payment gateway แทบทุกเจ้าในไทย
(Omise ที่ใช้อยู่ก็สตางค์) และตัวอย่างในเอกสารเขียน `10000` ซึ่งเท่ากับ ฿100 พอดี

**แต่ถ้าผมเดาผิด ทุกออเดอร์จะถูกเรียกเก็บเกินไป 100 เท่า** — ออเดอร์ ฿900 จะกลาย
เป็น ฿90,000

เพราะงั้น **สั่งของจริงราคาถูกที่สุด 1 ออเดอร์ แล้วจ่ายเอง** ดูว่าในแอปธนาคาร
ตัดเงินตรงกับยอดในออเดอร์เป๊ะมั้ย

- **ตรง** → ใช้ได้ เปิดให้ลูกค้าได้เลย
- **ไม่ตรง (เป็น 100 เท่า)** → บอกผม แก้บรรทัดเดียวจบ อยู่ที่
  `toMinorUnits()` ใน `api/paybeam.js` เปลี่ยน `* 100` เป็น `* 1`

ถ้า Beam มี **Playground** ให้ใช้ ทดสอบตรงนั้นก่อนก็ได้ ไม่ต้องเสียเงินจริง —
ใส่ `BEAM_API_BASE` เป็น URL ของ Playground ทดสอบเสร็จแล้วค่อยลบตัวแปรนั้นทิ้ง

---

## เรื่องที่ควรรู้ก่อน

**ร้านนี้ขายกัญชา** — payment gateway ทุกเจ้ามีรายการประเภทธุรกิจที่รับ/ไม่รับ
ที่ Omise ใช้เวลาเป็นเดือน อาจไม่ใช่เพราะเอกสารไม่ครบ แต่เพราะประเภทธุรกิจ
Beam ก็อาจตอบแบบเดียวกัน — **ถามเขาให้ชัดตั้งแต่แรกว่ารับร้านกัญชามั้ย** จะได้
ไม่เสียเวลาอีกเดือน

ถ้าติดตรงนี้ ระบบที่มีอยู่แล้วยังขายได้ปกติ: PromptPay QR แบบอัปสลิป · โอนธนาคาร ·
เงินสด/ปลายทาง · คริปโต

---

## MaxMe — ยังทำไม่ได้

ในโพสต์แนะนำ MaxMe มาด้วย ผมยังไม่ได้ทำ เพราะ:

1. หาเอกสาร API ของเขาไม่เจอเลย (ต่างจาก Beam ที่มี `docs.beamcheckout.com`)
2. คนที่แนะนำบอกเองว่าต้องให้เขา "cc ทีมงาน" ให้ก่อน = ต้องคุยกับคนก่อนถึงจะได้
   กุญแจ

ถ้าได้เอกสารหรือได้บัญชีแล้ว ส่งมา เดี๋ยวผมต่อให้ — โครงสร้างวางไว้แล้ว เพิ่ม
gateway ใหม่ใช้ไฟล์ 2 ไฟล์เหมือน `paybeam.js` + `beam-webhook.js`

ผมไม่เดา API ที่ไม่เคยเห็นเอกสารในโค้ดที่ตัดเงินลูกค้าครับ

---

## สำหรับคนที่มาแก้โค้ดต่อ

| ไฟล์ | ทำอะไร |
|---|---|
| `api/paybeam.js` | สร้างรายการเก็บเงิน (QR / บัตร) · เช็คสถานะ |
| `api/beam-webhook.js` | รับแจ้งจาก Beam ว่าจ่ายแล้ว → ทำเครื่องหมายในออเดอร์ |
| `api/pay-config.js` | บอกหน้าเว็บว่าเปิดเจ้าไหนอยู่ |
| `index.html` | `paymentMethods()` · `BeamQR` / `BeamCard` ใน `submitOrder()` |

กติกาที่ห้ามพัง:

- **ยอดเงินอ่านจากออเดอร์ที่บันทึกไว้ในเซิร์ฟเวอร์เสมอ** ไม่เคยเอาจากที่ browser
  ส่งมา — ไม่งั้นลูกค้าตั้งราคาเองได้ ทดสอบข้อนี้ไว้แล้วใน `beamtest.mjs`
- **webhook ต้องมี `?secret=`** ถ้า `WEBHOOK_SECRET` ไม่ได้ตั้ง ต้องปฏิเสธทุก
  request ไม่ใช่ปล่อยผ่าน (บั๊กเดิมของ `gbp-webhook.js` เขียนว่า `if (SECRET && …)`
  ซึ่งแปลว่าไม่ตั้ง = ไม่ตรวจ = ใครก็มาร์คออเดอร์ว่าจ่ายแล้วได้)
- **Beam ล่มต้องไม่ปิดร้าน** — `/api/paybeam` ตอบ 200 พร้อม `ok:false` ไม่ใช่ 500
  ออเดอร์บันทึกไปแล้ว หน้าเว็บจะบอกลูกค้าให้จ่ายปลายทางแทน
- **สถานะที่ถือว่าจ่ายแล้ว** อยู่ใน `PAID` regex ตัวเดียวใน `paybeam.js` และ
  `beam-webhook.js` ก็ import ตัวเดียวกันไปใช้ อย่าเขียนแยกสองที่

ทดสอบ: `beamtest.mjs` (API, 18 ข้อ) และ `beamui.js` (หน้าเว็บจริง) ใน scratchpad
